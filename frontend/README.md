## Frontend

This folder will contain the **web client** for FashionCart.

Suggested structure (to be created when implementing):

- `src/`
  - `components/`
  - `pages/` or `routes/`
  - `features/`
  - `services/`
  - `store/`
  - `styles/`
  - `utils/`
- `public/`

No implementation code has been added yet.

