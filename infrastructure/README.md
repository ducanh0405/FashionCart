## Infrastructure

This folder will hold configuration for **running and deploying** FashionCart.

Planned structure:

- `docker/`: Dockerfiles and docker-compose definitions.
- `k8s/`: Kubernetes manifests (if used).
- `ci-cd/`: CI/CD pipeline configuration.
- `monitoring/`: Logging, metrics, and alerting setup.
- `env-templates/`: `.env.example` and environment variable templates (no secrets).

No infrastructure code/config has been added yet.

